cluster:
  nodes:
    - id: node-0
      gpus:
        - id: gpu-0
          profile: H100_80GB
        - id: gpu-1
          profile: H100_80GB

scheduler:
  type: fifo

workload:
  path: ../workloads/inference_llama.yaml

hardware_profiles_dir: ../hardware
profiles_dir: ../profiles
