cluster:
  nodes:
    - id: node-0
      gpus:
        - id: gpu-0
          profile: H100_80GB

scheduler:
  type: preemptive

workload:
  path: ../workloads/preemption_demo.yaml

hardware_profiles_dir: ../hardware
