cluster:
  nodes:
    - id: node-a
      gpus:
        - { id: a-g0, profile: H100_80GB, nvlink_group: 0 }
        - { id: a-g1, profile: H100_80GB, nvlink_group: 1 }
scheduler:
  type: fifo
workload:
  path: ../workloads/topology_penalty_m5.yaml
hardware_profiles_dir: ../hardware
mig_profiles_dir: ../mig
