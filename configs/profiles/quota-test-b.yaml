# Test-only profile for the forge quota-enforcement lab scenario
# (../forge/scripts/test-lab-quota-enforcement.sh) — not real calibration
# data, just enough to let ZyForgeSim simulate the exported job.
model: quota-test-b
profiles:
  A100:
    runtime_seconds: 60
    gpu_memory_gb: 1
